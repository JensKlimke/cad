{
  "name": "@cad/tests-api",
  "version": "0.0.1",
  "private": true,
  "description": "Fastify harness + Supertest e2e tests. Slice 0 ships a /health endpoint stub so the harness itself is exercised; Slice 1 onward extends it with real project/document routes.",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint ."
  },
  "dependencies": {
    "@cad/db": "workspace:*",
    "@cad/protocol": "workspace:*",
    "@cad/references": "workspace:*",
    "@cad/server": "workspace:*",
    "fastify": "^5.8.5",
    "fastify-type-provider-zod": "^6.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cad/config": "workspace:*",
    "@cad/tests-containers": "workspace:*",
    "@types/node": "^25.8.0",
    "@types/supertest": "^7.2.0",
    "@vitest/coverage-v8": "^4.1.6",
    "supertest": "^7.2.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
