# @cad/references

Stable geometry handles and resolver utilities for CAD documents.

Slice 7 starts with semantic topology for sketch + pad documents. Handles serialize finder, construction, and hash selectors so UI picks can survive upstream parameter and sketch edits without relying on raw mesh indices.
