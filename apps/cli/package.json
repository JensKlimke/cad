{
  "name": "@cad/cli",
  "version": "0.0.1",
  "private": true,
  "description": "`cad` command-line interface for the CAD monorepo.",
  "type": "module",
  "sideEffects": false,
  "bin": {
    "cad": "./bin/cad.js"
  },
  "exports": {
    "./package.json": "./package.json"
  },
  "files": [
    "bin",
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "tsx scripts/gen-version.ts",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint ."
  },
  "dependencies": {
    "@cad/handbook": "workspace:*",
    "@cad/kernel": "workspace:*",
    "@cad/runtime": "workspace:*",
    "commander": "^14.0.3",
    "marked": "^15.0.12",
    "marked-terminal": "^7.3.0"
  },
  "devDependencies": {
    "@cad/config": "workspace:*",
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "^4.1.9",
    "rimraf": "^6.1.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
